# Rfq (spine-rfq)

Modul Spine — dokumen RFQ (customer → surveyor) dengan workflow status
draft/sent/accepted/declined/expired.

Dokumen porting: `~/wasnaker-porting-docs/SPINE-RQF.md`

- Fase 1: dokumen RFQ (customer → surveyor) + line items + equipment + workflow status
- Nanti: convert_to_quotation, PDF, acceptance publik (hash/signature), email template
- Tidak: billing/shipping, custom fields (YAGNI)
